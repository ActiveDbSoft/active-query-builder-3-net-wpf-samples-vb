<Button x:Class="Common.MenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Button.Resources>
        <Style TargetType="Image" x:Key="StyleImageButton">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Button.Resources>

    <Image x:Name="ImageContent" IsEnabled="{Binding RelativeSource={RelativeSource AncestorType=Button, Mode=FindAncestor, AncestorLevel=1}, Path=IsEnabled}"
                           Style="{StaticResource StyleImageButton}"/>
</Button>
