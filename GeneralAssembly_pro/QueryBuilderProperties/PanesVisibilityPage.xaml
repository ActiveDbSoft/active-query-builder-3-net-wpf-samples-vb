<UserControl x:Class="QueryBuilderProperties.PanesVisibilityPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             >
    <UserControl.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,3"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="4">
        <StackPanel>
            <CheckBox x:Name="cbShowDesignPane" Content="Show Design Pane"/>
            <CheckBox x:Name="cbShowQueryColumnsPane" Content="Show Query Columns Pane"/>
            <CheckBox x:Name="cbShowDatabaseSchemaView" Content="Show Database Schema View"/>
            <CheckBox x:Name="cbShowQueryNavigationBar" Content="Show Query Navigation Bar"/>
        </StackPanel>
    </Grid>
</UserControl>
