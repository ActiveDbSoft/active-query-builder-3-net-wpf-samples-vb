<Window x:Class="QueryStatisticsForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Query Statistics" Height="363" Width="486" WindowStartupLocation="CenterOwner" FontFamily="Microsoft Sans Serif" FontSize="8.25pt">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox IsReadOnly="True" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible" Grid.Row="0" x:Name="SqlTextBox"/>
        <Button Grid.Row="1" Width="75" Height="23" Content="OK" HorizontalAlignment="Right" Margin="0,7,0,0" x:Name="ButtonOk" Click="ButtonOk_OnClick"/>
    </Grid>
</Window>
